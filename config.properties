GET_PASSWORD=SELECT password FROM users WHERE email = ?
INITIALIZE_DB=CREATE TABLE IF NOT EXISTS users (uuid INT GENERATED BY DEFAULT AS IDENTITY, email VARCHAR(30), username VARCHAR(30), password VARCHAR(65), CONSTRAINT PK_users PRIMARY KEY (uuid));
USER_EXISTS=SELECT COUNT(email) AS total FROM users WHERE email = ?
INSERT_USER=INSERT INTO users(email, password, username) VALUES (?, ?, ?)
JDBC_URL=jdbc:postgresql://localhost:5432/postgres
DB_USER=postgres
DB_PASSWORD=738212
